module ${name} exposing (..)

{-| Elements from ${component} component. See https://www.webcomponents.org/element/PolymerElements/${component}

# Elements

${elements_doc_references}
-}

import Html exposing (Html, Attribute, node)
