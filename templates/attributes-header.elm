module ${name} exposing (..)

{-| Attributes from ${component} component. See https://www.webcomponents.org/element/PolymerElements/${component}

# Attributes

${elements_doc_references}
-}
import Html exposing (Attribute)
import Polymer.Attributes as Attributes
